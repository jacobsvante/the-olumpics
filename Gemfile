# These Ruby gems are only used for compiling the scss files to css.
# No need to install on server.

source 'https://rubygems.org'

gem 'sass', '>=3.3.0.rc.3', :group => :development
gem 'sass-css-importer', '>=1.0.0.beta.0', :group => :development
gem 'compass', '>=1.0.0.alpha.18', :group => :development
